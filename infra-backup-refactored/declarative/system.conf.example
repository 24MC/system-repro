# Declarative System Configuration
# Generated on: 2025-12-25 10:00:00
# This file defines what packages/services/configs SHOULD exist
# Edit this file to declare desired state, not current state

# Official packages (from Arch repositories)
# These will be installed using pacman
package.official.base=required
package.official.base-devel=required
package.official.linux=required
package.official.linux-firmware=required
package.official.vim=required
package.official.git=required
package.official.openssh=required
package.official.docker=required
package.official.docker-compose=required
package.official.networkmanager=required
package.official.sudo=required
package.official.pacman-contrib=required
package.official.htop=required
package.official.tree=required
package.official.ncdu=required
package.official.rsync=required
package.official.cronie=required

# AUR packages (from Arch User Repository)
# These require an AUR helper (yay, paru, etc.)
package.aur.yay=required
package.aur.visual-studio-code-bin=required
package.aur.google-chrome=required
package.aur.spotify=required

# System-wide services (managed by root)
# These will be enabled at boot time
service.system.sshd=enabled
service.system.NetworkManager=enabled
service.system.docker=enabled
service.system.cronie=enabled
service.system.ufw=enabled

# User services (managed by user)
# These will be enabled for the current user
service.user.pipewire=enabled
service.user.pipewire-pulse=enabled

# Masked services (explicitly disabled)
# These will remain masked during restore

# Service policy configuration
# Define how services should be handled during restore

# Critical services (start immediately after restore)
service.critical.network-manager=true
service.critical.ssh=true

# Configuration Files
# These configuration files SHOULD exist
config.system.etc_fstab.state=present
config.system.etc_fstab.source=inventory/config/etc_fstab
config.system.etc_fstab.checksum=d41d8cd98f00b204e9800998ecf8427e

config.system.etc_hosts.state=present
config.system.etc_hosts.source=inventory/config/etc_hosts
config.system.etc_hosts.checksum=d41d8cd98f00b204e9800998ecf8427e

config.home.user_.bashrc.state=present
config.home.user_.bashrc.source=inventory/config/home_user_.bashrc
config.home.user_.bashrc.checksum=d41d8cd98f00b204e9800998ecf8427e

# Package groups or patterns
# Example: package.group.base-devel=required
# Example: package.pattern.linux-lts=required

# Package exclusions (never install these)
# Example: package.exclude.unwanted-package=true

# Service dependencies (ensure these are started before)
service.before.docker.service=network-online.target